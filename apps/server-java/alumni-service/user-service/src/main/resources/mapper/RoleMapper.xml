<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cmswe.alumni.service.user.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.cmswe.alumni.common.entity.Role">
        <id property="roleId" column="role_id" jdbcType="BIGINT"/>
        <result property="roleName" column="role_name" jdbcType="VARCHAR"/>
        <result property="roleCode" column="role_code" jdbcType="VARCHAR"/>
        <result property="roleType" column="role_type" jdbcType="TINYINT"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="status" column="status" jdbcType="TINYINT"/>
        <result property="isDelete" column="is_delete" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        role_id, role_name, role_code, role_type, remark, create_time, update_time, status, is_delete
    </sql>

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectRolesByUserId" resultMap="BaseResultMap">
        SELECT r.*
        FROM role r
                 JOIN role_user ru ON r.role_id = ru.role_id
        WHERE ru.wx_id = #{userId}
          AND r.is_delete = 0
          AND ru.is_delete = 0
    </select>

    <!-- 根据角色名称查询角色 -->
    <select id="selectByRoleName" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role
        WHERE role_name = #{roleName} AND is_delete = 0
    </select>

    <!-- 根据角色代码查询角色 -->
    <select id="selectByRoleCode" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role
        WHERE role_code = #{roleCode} AND is_delete = 0
    </select>

</mapper>
