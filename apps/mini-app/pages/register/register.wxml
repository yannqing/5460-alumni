<!--pages/register/register.wxml-->
<view class="register-page">
  <!-- 自定义导航栏 -->
  <view class="custom-nav" style="padding-top: {{statusBarHeight}}px;">
    <view class="nav-content">
      <view class="back-btn" bindtap="goBack">
        <image class="back-icon" src="/assets/icons/back.png" mode="aspectFit"></image>
      </view>
    </view>
  </view>

  <!-- 主内容区域 -->
  <view class="main-content" style="padding-top: {{statusBarHeight + 44}}px;">
    <!-- 头部欢迎区域 -->
    <view class="header-section">
      <view class="welcome-text">
        <view class="welcome-line1">您好，欢迎使用</view>
        <view class="welcome-line2">5460校友服务平台</view>
      </view>
      <view class="illustration-wrapper">
        <image wx:if="{{illustration}}" class="illustration" src="{{illustration}}" mode="aspectFit"></image>
        <view wx:else class="illustration-placeholder">
          <text class="placeholder-icon">5460</text>
        </view>
      </view>
    </view>

    <!-- 表单区域 -->
    <view class="form-section">
      <!-- 真实姓名 -->
      <view class="form-item">
        <view class="form-label">真实姓名</view>
        <view class="form-input-wrapper">
          <input
            class="form-input"
            type="text"
            placeholder="请输入您的真实姓名"
            placeholder-class="input-placeholder"
            value="{{formData.realName}}"
            bindinput="onNameInput"
          />
        </view>
      </view>

      <!-- 性别 -->
      <view class="form-item">
        <view class="form-label">性别</view>
        <view class="gender-selector">
          <view
            class="gender-option {{formData.gender === 'male' ? 'active' : ''}}"
            bindtap="selectGender"
            data-gender="male"
          >
            <view class="gender-text">男</view>
          </view>
          <view
            class="gender-option {{formData.gender === 'female' ? 'active' : ''}}"
            bindtap="selectGender"
            data-gender="female"
          >
            <view class="gender-text">女</view>
          </view>
        </view>
      </view>

      <!-- 大学 -->
      <view class="form-item">
        <view class="form-label">大学</view>
        <picker
          mode="selector"
          range="{{universityList}}"
          value="{{universityIndex}}"
          bindchange="onUniversityChange"
        >
          <view class="form-picker">
            <text class="{{formData.university ? 'picker-value' : 'picker-placeholder'}}">
              {{formData.university || '请选择您的大学'}}
            </text>
            <text class="picker-arrow-text">></text>
          </view>
        </picker>
      </view>

      <!-- 手机号 -->
      <view class="form-item">
        <view class="form-label">手机号</view>
        <view class="form-input-wrapper">
          <input
            class="form-input"
            type="number"
            maxlength="11"
            placeholder="请输入您的手机号"
            placeholder-class="input-placeholder"
            value="{{formData.phone}}"
            bindinput="onPhoneInput"
          />
        </view>
      </view>
    </view>

    <!-- 用户协议 -->
    <view class="agreement-section">
      <view class="agreement-checkbox" bindtap="toggleAgreement">
        <view class="checkbox-icon {{isAgreed ? 'checked' : ''}}">
          <text wx:if="{{isAgreed}}" class="check-text">✓</text>
        </view>
      </view>
      <view class="agreement-text">
        <text>我已阅读</text>
        <text class="link" bindtap="viewUserAgreement">用户协议</text>
        <text>和</text>
        <text class="link" bindtap="viewPrivacyPolicy">隐私政策</text>
        <text>并理解相关条款</text>
      </view>
    </view>

    <!-- 注册按钮 -->
    <view class="submit-section">
      <button
        class="submit-btn {{isFormValid ? 'active' : 'disabled'}}"
        bindtap="submitRegister"
      >
        注册
      </button>
    </view>
  </view>
</view>
