<!--pages/local-platform/list/list.wxml-->
<!-- 消息通知组件 -->
<message-notification />

<view class="container">
  <!-- 搜索栏 -->
  <view class="search-bar">
    <view class="search-input-wrap">
      <input class="search-input" type="text" placeholder="搜索校处会名称" value="{{keyword}}" bindinput="onSearchInput" bindconfirm="onSearch" />
      <image class="search-icon" src="{{iconSearch}}" mode="aspectFit" bindtap="onSearch" />
    </view>
  </view>

  <!-- 筛选栏 -->
  <view class="filter-bar">
    <view class="filter-item" wx:for="{{filters}}" wx:key="label" data-index="{{index}}" bindtap="openFilterOptions">
      <text>{{item.options[item.selected]}}</text>
      <text class="filter-arrow">▾</text>
    </view>
  </view>

  <view wx:if="{{showFilterOptions}}">
    <view class="filter-mask" bindtap="closeFilterOptions"></view>
    <view class="filter-options-panel">
      <view class="option-item {{optIndex === filters[activeFilterIndex].selected ? 'active' : ''}}" wx:for="{{filters[activeFilterIndex].options}}" wx:key="*this" wx:for-index="optIndex" data-option-index="{{optIndex}}" bindtap="selectFilterOption">
        {{item}}
      </view>
    </view>
  </view>

  <!-- 校处会列表 -->
  <view class="platform-list">
    <view class="platform-card" wx:for="{{platformList}}" wx:key="platformId" bindtap="viewDetail" data-id="{{item.platformId}}">
      <!-- 头部：头像、名称 -->
      <view class="card-header">
        <image class="platform-avatar" src="{{item.avatar}}" mode="aspectFill" />
        <view class="card-info">
          <view class="card-title-row">
            <text class="card-title">{{item.platformName}}</text>
            <view wx:if="{{item.status === 1}}" class="status-badge active">活跃</view>
            <view wx:else class="status-badge inactive">未激活</view>
          </view>

          <!-- 城市信息 -->
          <view class="card-subtitle">
            <image class="subtitle-icon" src="{{iconLocation}}" mode="aspectFit" />
            <text>{{item.city}}</text>
          </view>

          <!-- 范围信息 -->
          <view class="card-scope" wx:if="{{item.scope}}">
            <image class="scope-icon" src="{{iconScope}}" mode="aspectFit" />
            <text>{{item.scope}}</text>
          </view>

          <!-- 联系方式 -->
          <view class="card-contact" wx:if="{{item.contactInfo}}">
            <image class="contact-icon" src="{{iconContact}}" mode="aspectFit" />
            <text>{{item.contactInfo}}</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 加载状态 -->
    <view wx:if="{{loading}}" class="loading-text">加载中...</view>
    <view wx:elif="{{!hasMore}}" class="loading-text">没有更多了</view>

    <!-- 空状态 -->
    <view wx:if="{{!loading && platformList.length === 0}}" class="empty-state">
      <view class="empty-placeholder">暂无数据</view>
      <text>暂无数据</text>
    </view>
  </view>
</view>
