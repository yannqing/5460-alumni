<!--pages/shop/list/list.wxml-->
<view class="container shop-page">
  <view class="shop-hero card">
    <view class="hero-header">
      <view>
        <view class="hero-title">校友商铺</view>
        <view class="hero-subtitle">校友经营的优质商铺</view>
      </view>
    </view>
    <view class="search-bar">
      <text class="icon">🔍</text>
      <input
        class="search-input"
        placeholder="搜索商铺名称/类别/地址"
        value="{{searchValue}}"
        bindinput="handleSearchInput"
      />
    </view>
  </view>

  <scroll-view class="filter-tabs" scroll-x>
    <view
      class="tab {{filterCategory === option.id ? 'active' : ''}}"
      wx:for="{{filterOptions}}"
      wx:key="id"
      data-id="{{option.id}}"
      bindtap="handleFilterChange"
    >
      {{option.label}}
    </view>
  </scroll-view>

  <view wx:if="{{!displayList.length}}" class="empty-block card">
    <text>暂无商铺信息</text>
  </view>

  <view wx:else class="shop-list">
    <view
      class="shop-card card"
      wx:for="{{displayList}}"
      wx:key="id"
      bindtap="viewDetail"
      data-id="{{item.id}}"
    >
      <image class="shop-cover" src="{{item.cover}}" mode="aspectFill" />
      <view class="shop-content">
        <view class="shop-name">{{item.name}}</view>
        <view class="shop-desc">{{item.description}}</view>
        <view class="shop-meta">
          <view class="meta-item">
            <text class="rating">⭐ {{item.rating}}</text>
            <text class="meta-divider">·</text>
            <text class="category">{{item.category}}</text>
          </view>
          <view class="meta-item">
            <image class="location-icon" src="{{iconLocation}}" mode="aspectFit" />
            <text>{{item.location}}</text>
            <text class="distance">距离{{item.distance}}</text>
          </view>
        </view>
        <view class="shop-owner">
          <text class="owner-label">店主：</text>
          <text class="owner-name">{{item.owner}}</text>
        </view>
      </view>
    </view>
  </view>
</view>

