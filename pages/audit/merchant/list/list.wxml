<!--pages/audit/merchant/list/list.wxml-->
<view class="merchant-audit-list-page">
  <!-- 标签栏 -->
  <view class="tabs-container">
    <view 
      class="tab-item {{currentTab === index ? 'active' : ''}}"
      wx:for="{{tabs}}"
      wx:key="index"
      wx:for-index="index"
      data-index="{{index}}"
      bindtap="switchTab"
    >
      {{item}}
    </view>
  </view>

  <!-- 商家列表 -->
  <view class="merchant-list">
    <view 
      class="merchant-item"
      wx:for="{{merchantList}}"
      wx:key="id"
      data-id="{{item.id}}"
      bindtap="viewDetail"
    >
      <view class="merchant-header">
        <view class="merchant-name">{{item.name}}</view>
        <view class="merchant-status {{item.status}}">{{item.statusText}}</view>
      </view>
      <view class="merchant-info">
        <text class="info-label">负责人：</text>
        <text class="info-value">{{item.owner}}</text>
      </view>
      <view class="merchant-info">
        <text class="info-label">联系电话：</text>
        <text class="info-value">{{item.phone}}</text>
      </view>
      <view class="merchant-info">
        <text class="info-label">地址：</text>
        <text class="info-value">{{item.address}}</text>
      </view>
      <view class="merchant-info">
        <text class="info-label">提交时间：</text>
        <text class="info-value">{{item.submitTime}}</text>
      </view>
    </view>

    <!-- 空状态 -->
    <view class="empty-state" wx:if="{{!loading && merchantList.length === 0}}">
      <text class="empty-text">暂无商家审核数据</text>
    </view>

    <!-- 加载中 -->
    <view class="loading-state" wx:if="{{loading}}">
      <text class="loading-text">加载中...</text>
    </view>
  </view>
</view>

