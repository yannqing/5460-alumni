/* pages/audit/merchant/topic/edit/edit.wxss */
.activity-edit-page {
  min-height: 100vh;
  background-color: #f5f5f5;
  padding-top: 160rpx; /* 导航栏再次下移后，继续增加padding-top值 */
}

/* 顶部导航栏 */
.nav-bar {
  position: fixed;
  top: 60rpx;
  left: 0;
  right: 0;
  height: 100rpx;
  background-color: #fff;
  border-bottom: 1rpx solid #eee;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 30rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
  z-index: 100;
}

.nav-left, .nav-right {
  width: 80rpx;
  display: flex;
  align-items: center;
}

.nav-left {
  justify-content: flex-start;
}

.nav-right {
  justify-content: flex-end;
}

.nav-icon {
  font-size: 40rpx;
  color: #333;
}

.nav-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
}

/* 加载状态 */
.loading-container {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 60rpx;
  background-color: #fff;
  margin: 30rpx;
  border-radius: 10rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
}

.loading-text {
  font-size: 28rpx;
  color: #999;
}

/* 表单容器 */
.form-container {
  padding: 30rpx;
}

/* 表单项 */
.form-item {
  margin-bottom: 30rpx;
  background-color: #fff;
  padding: 30rpx;
  border-radius: 10rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
}

/* 表单标签 */
.form-label {
  display: block;
  font-size: 28rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 16rpx;
}

.form-label.required::after {
  content: '*';
  color: #ff4d4f;
  margin-left: 8rpx;
}

/* 表单输入框 */
.form-input {
  width: 100%;
  padding: 32rpx 24rpx;
  font-size: 28rpx;
  color: #333;
  border: 1rpx solid #e5e5e5;
  border-radius: 10rpx;
  background-color: #fafafa;
  box-sizing: border-box;
  transition: all 0.3s ease;
  letter-spacing: 0.5rpx;
}

.form-input:focus {
  border-color: #667eea;
  background-color: #fff;
}

/* 表单文本域 */
.form-textarea {
  width: 100%;
  min-height: 200rpx;
  padding: 32rpx 24rpx;
  font-size: 28rpx;
  color: #333;
  border: 1rpx solid #e5e5e5;
  border-radius: 10rpx;
  background-color: #fafafa;
  box-sizing: border-box;
  resize: vertical;
  transition: all 0.3s ease;
  line-height: 1.6;
  letter-spacing: 0.5rpx;
}

.form-textarea:focus {
  border-color: #667eea;
  background-color: #fff;
}

/* 表单选择器 */
.form-picker {
  width: 100%;
  padding: 32rpx 24rpx;
  border: 1rpx solid #e5e5e5;
  border-radius: 10rpx;
  font-size: 28rpx;
  color: #333;
  box-sizing: border-box;
  background-color: #fafafa;
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: all 0.3s ease;
  letter-spacing: 0.5rpx;
}

.form-picker:active {
  background-color: #e8e8e8;
}

.form-picker::after {
  content: '▼';
  font-size: 24rpx;
  color: #999;
}

/* 单选按钮组 */
.radio-group {
  display: flex;
  align-items: center;
}

.radio-item {
  display: flex;
  align-items: center;
  margin-right: 60rpx;
  font-size: 28rpx;
  color: #666;
}

.radio-item text {
  margin-left: 10rpx;
}

/* 图片上传容器 */
.upload-container {
  margin-top: 16rpx;
}

/* 图片列表 */
.image-list {
  display: flex;
  flex-wrap: wrap;
  margin-bottom: 16rpx;
}

/* 图片项 */
.image-item {
  position: relative;
  width: 160rpx;
  height: 160rpx;
  margin-right: 20rpx;
  margin-bottom: 20rpx;
  border-radius: 10rpx;
  overflow: hidden;
  border: 1rpx solid #eee;
}

/* 列表图片 */
.list-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* 上传按钮 */
.upload-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 160rpx;
  height: 160rpx;
  border: 2rpx dashed #ddd;
  border-radius: 10rpx;
  background-color: #f9f9f9;
  cursor: pointer;
  transition: all 0.3s ease;
}

.upload-btn:hover {
  border-color: #667eea;
  background-color: #f0f4ff;
}

.upload-btn:active {
  transform: scale(0.98);
}

/* 上传图标 */
.upload-icon {
  font-size: 48rpx;
  color: #999;
  margin-bottom: 8rpx;
}

/* 上传文本 */
.upload-text {
  font-size: 22rpx;
  color: #666;
}

/* 图片删除按钮 */
.image-delete-btn {
  position: absolute;
  top: 0;
  right: 0;
  width: 50rpx;
  height: 50rpx;
  background-color: rgba(255, 69, 69, 0.9);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 2rpx 10rpx rgba(255, 69, 69, 0.3);
  transition: all 0.3s ease;
}

/* 图片上传中状态 */
.image-loading {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 10rpx;
}

.image-loading .loading-text {
  color: #fff;
  font-size: 24rpx;
}

.image-delete-btn:active {
  transform: scale(0.9);
  background-color: rgba(255, 69, 69, 1);
}

/* 删除图标 */
.delete-icon {
  color: #fff;
  font-size: 36rpx;
  font-weight: bold;
  line-height: 1;
}

/* 图片数量提示 */
.image-count {
  font-size: 24rpx;
  color: #999;
  display: block;
}

/* 时间选择器样式 */
.form-picker-view {
  width: 100%;
  height: 300rpx;
  background-color: #fff;
  border: 1rpx solid #eee;
  border-radius: 10rpx;
  margin-bottom: 16rpx;
}

.picker-view-item {
  font-size: 28rpx;
  color: #333;
  height: 50rpx;
  line-height: 50rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 表单提示文字 */
.form-hint {
  font-size: 24rpx;
  color: #999;
  margin-top: 8rpx;
}

/* 提交按钮容器 */
.submit-btn-container {
  margin-top: 40rpx;
  padding: 0 30rpx;
  margin-bottom: 30rpx;
}

/* 提交按钮 */
.submit-btn {
  width: 100%;
  height: 90rpx;
  background-color: #667eea;
  color: #fff;
  font-size: 32rpx;
  font-weight: bold;
  border-radius: 10rpx;
  border: none;
  box-shadow: 0 2rpx 10rpx rgba(102, 126, 234, 0.3);
  transition: all 0.3s ease;
}

.submit-btn:hover {
  background-color: #5a6fd8;
  box-shadow: 0 4rpx 15rpx rgba(102, 126, 234, 0.4);
}

.submit-btn:active {
  transform: scale(0.98);
}

.submit-btn:disabled {
  background-color: #ccc;
  box-shadow: none;
  cursor: not-allowed;
  opacity: 0.6;
}

/* 位置选择器样式 */
.location-selector {
  display: flex;
  gap: 16rpx;
  align-items: center;
}

.location-input {
  flex: 1;
  background-color: white;
  color: #666;
}

.select-location-btn {
  width: 200rpx;
  height: 80rpx;
  background-color: #1AAD19;
  color: white;
  font-size: 28rpx;
  border: none;
  border-radius: 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

.select-location-btn:active {
  background-color: #179B16;
}

/* 去除按钮默认样式 */
.select-location-btn::after {
  border: none;
}