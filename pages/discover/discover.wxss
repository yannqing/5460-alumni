/* pages/discover/discover.wxss */
.discover-page {
  height: 100vh;
  background-color: #f5f5f5;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* 蓝色头部 */
.header-section {
  background-color: #4a90e2;
  padding: 0;
  padding-top: env(safe-area-inset-top);
  padding-bottom: 40rpx;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 0;
}

.header-section-inner {
  padding:100rpx 30rpx 0;
}

.header-top-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
}

.header-actions {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.action-btn {
  width: 60rpx;
  height: 60rpx;
  background-color: rgba(255, 255, 255, 0.25);
  border-radius: 30rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 28rpx;
  flex-shrink: 0;
}

.logo-placeholder {
  width: 130rpx;
  height: 60rpx;
  border-radius: 30rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.logo-icon {
  width: 130rpx;
  height: 60rpx;
  display: block;
}

.search-container {
  display: flex;
  align-items: center;
  background-color: rgba(255, 255, 255, 0.15);
  border-radius: 50rpx;
  padding: 12rpx 18rpx;
  gap: 16rpx;
}

.search-icon {
  font-size: 32rpx;
  flex-shrink: 0;
  color: rgba(255, 255, 255, 0.8);
}

.search-input {
  flex: 1;
  font-size: 28rpx;
  color: #fff;
  background-color: transparent;
}

.search-input::placeholder {
  color: #fff;
}

.search-btn {
  padding: 10rpx 28rpx;
  background-color: rgba(54, 168, 202, 0.8);
  color: #fff;
  border-radius: 40rpx;
  font-size: 26rpx;
  flex-shrink: 0;
  white-space: nowrap;
}

/* 白色内容容器 */
.white-content-container {
  background-color: #fff;
  border-top-left-radius: 32rpx;
  border-top-right-radius: 32rpx;
  overflow: hidden;
  position: fixed;
  top: calc(env(safe-area-inset-top) + 350rpx);
  left: 0;
  right: 0;
  z-index: 10;
}

/* 导航标签 */
.nav-tabs {
  display: flex;
  padding: 30rpx 30rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.nav-tab {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  padding: 20rpx 8rpx;
  border-radius: 8rpx;
  margin: 0 4rpx;
}

.nav-tab.active {
  background-color: rgba(74, 144, 226, 0.1);
}

.tab-icon {
  font-size: 32rpx;
  color: #333;
}

.tab-icon-img {
  width: 60rpx;
  height: 60rpx;
  display: block;
}

.nav-tab.active .tab-icon {
  color: #4a90e2;
}

.tab-text {
  font-size: 24rpx;
  color: #666;
}

.nav-tab.active .tab-text {
  color: #4a90e2;
  font-weight: 500;
}

/* 视图切换和排序容器 */
.view-sort-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 30rpx;
  gap: 20rpx;
  position: fixed;
  top: calc(env(safe-area-inset-top) + 345rpx + 180rpx);
  left: 0;
  right: 0;
  z-index: 20;
  border-bottom: 1rpx solid #f0f0f0;
}

.view-toggle {
  display: flex;
  gap: 16rpx;
  flex-shrink: 0;
  margin-left: auto;
}

.toggle-item {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  padding: 8rpx 16rpx;
  border-radius: 8rpx;
  background-color: #f5f5f5;
  gap: 4rpx;
}

.toggle-item.active {
  background-color: #4a90e2;
}

.toggle-icon {
  font-size: 30rpx;
  line-height: 1;
}

.toggle-text {
  font-size: 25rpx;
  color: #666;
  line-height: 1;
}

.toggle-item.active .toggle-text {
  color: #fff;
}

.sort-divider {
  width: 1rpx;
  height: 40rpx;
  background-color: #e0e0e0;
  flex-shrink: 0;
}

.sort-options {
  display: flex;
  align-items: center;
  gap: 30rpx;
  flex-shrink: 0;
}

/* 列表视图 */
.list-view {
  flex: 1;
  height: 0;
  margin-top: calc(env(safe-area-inset-top) + 345rpx + 120rpx + 135rpx);
  position: relative;
  z-index: 20;
  background-color: #f5f5f5;
  overflow-y: auto;
}

.sort-item {
  font-size: 26rpx;
  color: #999;
  padding: 8rpx 0;
}

.sort-item.active {
  color: #4a90e2;
  font-weight: 600;
}

/* 内容列表 */
.content-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding: 20rpx 30rpx;
  min-height: 200rpx;
}

/* 空列表提示 */
.empty-tip {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 0;
  color: #999;
  font-size: 28rpx;
  gap: 20rpx;
}

.empty-tip::before {
  content: "💡";
  font-size: 64rpx;
  opacity: 0.6;
}

/* 没有更多了提示 */
.no-more {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 50rpx 0;
  color: #999;
  font-size: 26rpx;
  background-color: #fff;
  border-radius: 12rpx;
  margin: 0 30rpx 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

/* 加载状态 */
.loading {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40rpx 0;
  color: #666;
  font-size: 26rpx;
}

.loading::before {
  content: "";
  width: 32rpx;
  height: 32rpx;
  border: 4rpx solid #f0f0f0;
  border-top: 4rpx solid #4a90e2;
  border-radius: 50%;
  margin-right: 12rpx;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

/* 商铺卡片 */
.shop-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
  overflow: hidden;
}

.shop-main {
  display: flex;
  gap: 20rpx;
  margin-bottom: 20rpx;
}

.shop-image {
  width: 140rpx;
  height: 140rpx;
  border-radius: 12rpx;
  background: #f5f5f5;
  flex-shrink: 0;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
}

.shop-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

/* 商铺名称行 */
.shop-name-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex-wrap: wrap;
}

.shop-name {
  font-size: 32rpx;
  font-weight: 600;
  color: #262626;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.recommend-tag {
  padding: 4rpx 16rpx;
  background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
  color: #fff;
  border-radius: 20rpx;
  font-size: 22rpx;
  font-weight: 500;
  flex-shrink: 0;
}

/* 商铺元信息 */
.shop-meta {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex-wrap: wrap;
  font-size: 24rpx;
  color: #8c8c8c;
}

.shop-rating {
  display: flex;
  align-items: center;
  gap: 4rpx;
}

.rating-star {
  font-size: 22rpx;
  color: #ffd700;
}

.rating-score {
  color: #ff9800;
  font-weight: 500;
}

.rating-count {
  color: #999;
  font-size: 22rpx;
}

.shop-view-count {
  color: #999;
  font-size: 22rpx;
}

/* 商铺标签 */
.shop-tags {
  display: flex;
  gap: 10rpx;
  flex-wrap: wrap;
}

.tag {
  padding: 6rpx 16rpx;
  background: #f0f7ff;
  color: #4a90e2;
  border-radius: 16rpx;
  font-size: 22rpx;
  border: 1rpx solid #e1f0ff;
}

/* 商铺详情信息 */
.shop-details {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  padding: 16rpx;
  background-color: #f8f9fa;
  border-radius: 12rpx;
  margin-bottom: 20rpx;
  border: 1rpx solid #e9ecef;
}

.shop-description {
  font-size: 26rpx;
  color: #595959;
  line-height: 1.6;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.shop-contact, .shop-hours {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 24rpx;
  color: #666;
}

.contact-icon, .hours-icon {
  font-size: 24rpx;
  flex-shrink: 0;
}

/* 优惠券列表 */
.coupons {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

/* 优惠券卡片 */
.coupon {
  display: flex;
  align-items: stretch;
  padding: 20rpx;
  background: linear-gradient(135deg, #fff8f0 0%, #fff2e8 100%);
  border-radius: 12rpx;
  border-left: 8rpx solid #ff6b6b;
  position: relative;
  overflow: hidden;
}

/* 优惠券左侧 - 折扣信息 */
.coupon-left {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  min-width: 160rpx;
  padding-right: 20rpx;
  border-right: 1px dashed #ffe0cc;
}

.coupon-value {
  font-size: 40rpx;
  font-weight: 700;
  color: #ff6b6b;
  line-height: 1;
}

.coupon-type {
  font-size: 24rpx;
  color: #ff874d;
  font-weight: 500;
}

.coupon-alumni-tag {
  padding: 4rpx 12rpx;
  background: #ffe0cc;
  color: #ff6b6b;
  border-radius: 12rpx;
  font-size: 20rpx;
  font-weight: 500;
  margin-top: 8rpx;
}

/* 优惠券中间 - 标题和有效期 */
.coupon-middle {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 12rpx;
  padding: 0 20rpx;
}

.coupon-title {
  font-size: 28rpx;
  color: #333;
  font-weight: 500;
  line-height: 1.4;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.coupon-expire {
  font-size: 22rpx;
  color: #999;
}

/* 优惠券右侧 - 剩余数量和领取按钮 */
.coupon-right {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  justify-content: center;
  gap: 12rpx;
  padding-left: 20rpx;
  border-left: 1px dashed #ffe0cc;
}

.coupon-remain {
  font-size: 22rpx;
  color: #666;
}

.coupon-remain-low {
  color: #ff6b6b;
  font-weight: 500;
}

.coupon-remain-empty {
  color: #999;
  font-weight: 500;
}

.coupon-btn {
  padding: 12rpx 32rpx;
  background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
  color: #fff;
  border-radius: 24rpx;
  font-size: 26rpx;
  font-weight: 500;
  box-shadow: 0 2rpx 8rpx rgba(255, 107, 107, 0.3);
  transition: all 0.3s ease;
}

.coupon-btn:active {
  transform: scale(0.95);
  box-shadow: 0 1rpx 4rpx rgba(255, 107, 107, 0.2);
}

/* 已抢光按钮样式 */
.coupon-btn-disabled {
  padding: 12rpx 32rpx;
  background: #f5f5f5;
  color: #999;
  border-radius: 24rpx;
  font-size: 26rpx;
  font-weight: 500;
  cursor: not-allowed;
}

/* 优惠券悬停效果 */
.coupon:hover {
  transform: translateY(-2rpx);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.12);
  transition: all 0.3s ease;
}

.coupon-btn:hover {
  background: linear-gradient(135deg, #ee5a24 0%, #ff6b6b 100%);
}

/* 响应式调整 */
@media (max-width: 375px) {
  .coupon {
    flex-direction: column;
    gap: 20rpx;
    text-align: center;
  }
  
  .coupon-left, .coupon-middle, .coupon-right {
    border: none;
    padding: 0;
  }
  
  .coupon-right {
    align-items: center;
  }
}

/* 场所卡片 */
.venue-card {
  background-color: #fff;
  border-radius: 16rpx;
  overflow: hidden;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
}

.venue-image {
  width: 100%;
  height: 320rpx;
  background-color: #f5f5f5;
  display: block;
}

.venue-content {
  padding: 24rpx;
}

.venue-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16rpx;
}

.venue-name {
  font-size: 32rpx;
  font-weight: 600;
  color: #262626;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.venue-type-tag {
  padding: 6rpx 16rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
  border-radius: 20rpx;
  font-size: 22rpx;
  flex-shrink: 0;
  margin-left: 12rpx;
}

.venue-address {
  display: flex;
  align-items: flex-start;
  gap: 8rpx;
  margin-bottom: 16rpx;
  padding: 12rpx;
  background-color: #f8f9fa;
  border-radius: 8rpx;
}

.venue-icon {
  font-size: 24rpx;
  flex-shrink: 0;
  line-height: 1.5;
}

.venue-address-text {
  font-size: 26rpx;
  color: #595959;
  line-height: 1.5;
  flex: 1;
}

.venue-info-row {
  display: flex;
  align-items: center;
  gap: 24rpx;
  margin-bottom: 16rpx;
  flex-wrap: wrap;
}

.venue-distance,
.venue-contact {
  display: flex;
  align-items: center;
  gap: 6rpx;
  font-size: 24rpx;
  color: #8c8c8c;
}

.venue-stats {
  display: flex;
  align-items: center;
  gap: 24rpx;
  padding: 16rpx 0;
  border-top: 1rpx solid #f0f0f0;
  border-bottom: 1rpx solid #f0f0f0;
  margin-bottom: 16rpx;
}

.stat-item {
  display: flex;
  align-items: center;
  gap: 6rpx;
}

.stat-icon {
  font-size: 24rpx;
}

.stat-text {
  font-size: 24rpx;
  color: #595959;
}

.venue-description {
  font-size: 26rpx;
  color: #8c8c8c;
  line-height: 1.6;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

/* 校友/内容卡片 */
.content-card {
  background-color: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  display: flex;
  align-items: center;
  gap: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

.card-avatar {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  background-color: #f0f0f0;
  overflow: hidden;
  flex-shrink: 0;
}

.avatar-img {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

.card-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.card-name {
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
}

.card-distance {
  font-size: 24rpx;
  color: #666;
}

.card-signature {
  font-size: 24rpx;
  color: #999;
  margin-top: 4rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-association {
  font-size: 24rpx;
  color: #999;
  margin-top: 4rpx;
}

.card-tag {
  font-size: 22rpx;
  color: #4a90e2;
  margin-top: 4rpx;
}

.card-action {
  flex-shrink: 0;
}

.follow-btn {
  padding: 12rpx 32rpx;
  background-color: #4a90e2;
  color: #fff;
  border-radius: 40rpx;
  font-size: 26rpx;
  text-align: center;
}

.follow-btn.followed {
  background-color: #f0f0f0;
  color: #666;
}

/* 活动卡片 */
.activity-card {
  background-color: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
  position: relative;
}

.activity-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 12rpx;
}

.activity-date {
  font-size: 26rpx;
  color: #666;
  margin-bottom: 12rpx;
}

.activity-association {
  font-size: 26rpx;
  color: #999;
  margin-bottom: 16rpx;
}

.activity-participants {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.participant-avatars {
  display: flex;
  gap: -10rpx;
}

.participant-avatar {
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  border: 2rpx solid #fff;
  margin-left: -10rpx;
  background-color: #f0f0f0;
  overflow: hidden;
}

.participant-avatar:first-child {
  margin-left: 0;
}

.avatar-small {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

.participant-text {
  font-size: 24rpx;
  color: #666;
}

.activity-actions {
  margin-bottom: 20rpx;
}

.signup-btn {
  padding: 16rpx 0;
  background-color: #4a90e2;
  color: #fff;
  border-radius: 8rpx;
  text-align: center;
  font-size: 28rpx;
  font-weight: 500;
}

.activity-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 16rpx;
  border-top: 1rpx solid #f0f0f0;
}

.activity-location {
  font-size: 24rpx;
  color: #999;
}

.activity-icons {
  display: flex;
  gap: 20rpx;
}

.icon-item {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-like,
.icon-share {
  font-size: 32rpx;
}

.activity-status {
  position: absolute;
  bottom: 24rpx;
  right: 24rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.signed-btn {
  padding: 8rpx 20rpx;
  background-color: #f5f5f5;
  color: #666;
  border-radius: 40rpx;
  font-size: 24rpx;
}

.signed-count {
  font-size: 24rpx;
  color: #666;
}

/* 地图视图 */
.map-view {
  flex: 1;
  height: 0;
  margin-top: calc(env(safe-area-inset-top) + 345rpx + 180rpx + 60rpx);
  position: relative;
  z-index: 10;
  background-color: #f5f5f5;
}

.discover-map {
  width: 100%;
  height: 100%;
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
}

/* 定位按钮 */
.location-btn {
  position: absolute;
  right: 30rpx;
  bottom: 150rpx;
  width: 80rpx;
  height: 80rpx;
  background-color: #fff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.15);
  z-index: 100;
}

.location-icon {
  font-size: 40rpx;
  line-height: 1;
}

/* 隐藏的 Canvas */
.hidden-canvas {
  position: absolute;
  top: -9999rpx;
  left: -9999rpx;
  width: 50px;
  height: 50px;
  opacity: 0;
  pointer-events: none;
}

/* 圆形头像标记 */
.marker-avatar {
  width: 50rpx;
  height: 50rpx;
  border-radius: 50%;
  border: 2rpx solid #fff;
  box-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.2);
}
